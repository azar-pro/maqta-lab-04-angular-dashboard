import { Component, inject, signal } from '@angular/core';
import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';
import { BusinessDataService } from '../../core/services/business-data.service';
@Component({selector:'app-settings',standalone:true,imports:[ReactiveFormsModule],template:`
<div class="page-head"><div><div class="eyebrow">WORKSPACE</div><h1>Settings</h1><p>Workspace defaults, notifications and regional preferences.</p></div></div>
<form class="panel settings-form" [formGroup]="form" (ngSubmit)="save()"><label>Workspace name<input formControlName="name">@if(form.controls.name.touched&&form.controls.name.invalid){<small class="form-hint error-text">Workspace name is required.</small>}</label><label>Currency<select formControlName="currency"><option>USD</option><option>SAR</option><option>MAD</option><option>EUR</option></select></label><label>Timezone<select formControlName="timezone"><option>America/New_York</option><option>Asia/Riyadh</option><option>Africa/Casablanca</option><option>Europe/London</option></select></label><label class="check-setting"><input type="checkbox" formControlName="riskAlerts"><span><strong>Project risk alerts</strong><small>Notify operations when a project receives a delivery or budget risk.</small></span></label><label class="check-setting"><input type="checkbox" formControlName="stockAlerts"><span><strong>Low-stock alerts</strong><small>Notify when material reaches its reorder threshold.</small></span></label><div class="settings-actions"><button class="primary" type="submit" [disabled]="form.invalid">Save changes</button>@if(saved()){<span class="save-success" role="status">Changes saved locally.</span>}</div></form>
  <section class="panel settings-qa"><div><span class="panel-kicker">PORTFOLIO QA</span><h2>State preview</h2><p>Trigger the controlled repository failure to verify the global error and retry experience.</p></div><button class="secondary" type="button" (click)="data.simulateNextLoadFailure()">Simulate load error</button></section>`})
export class SettingsComponent{readonly data=inject(BusinessDataService);readonly saved=signal(false);readonly form=new FormBuilder().nonNullable.group({name:['RIVET Operations',Validators.required],currency:'USD',timezone:'America/New_York',riskAlerts:true,stockAlerts:true});save(){if(this.form.invalid){this.form.markAllAsTouched();return;}localStorage.setItem('rivet-settings',JSON.stringify(this.form.getRawValue()));this.saved.set(true);window.setTimeout(()=>this.saved.set(false),2200);}}